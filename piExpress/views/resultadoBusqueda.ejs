<!DOCTYPE html>
<html lang="en">

<%-include("./partials/head")%>

    <body>
        <% if (locals.user != undefined) { %>
            <%- include('./partials/headerLogueado') %>
          <% } else { %>
            <%- include('./partials/header') %>
          <% } %>
    
            <main>
                <section class="center main-content">
                    <h1 class="tituloFormulario">Resultados de búsqueda.</h1>
                    <% for( let i = 0; i < posts.length; i++ ) { %>
                        <div>
                            <a href="/users/detalleUsuario/<%=posts[i].usuarios_id_posteo.id_usuario%>">
                                <h2>
                                    <%= posts[i].usuarios_id_posteo.email %> 
                                </h2></a>

                            <a href="/products/detallePost/<%=posts[i].id_posteo%>"><img class="imagenResultadoBusqueda" src="<%= posts[i].nombre_imagen%>" alt="foto de perfil"> </a>
                        </div>
                    <% } %>
                    
                </section>
            </main>
            <%-include("./partials/footer")%>

    </body>

</html>